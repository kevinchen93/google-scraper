{
  "name": "google-scraper",
  "version": "1.0.0",
  "description": "",
  "main": "query.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "puppeteer": "^1.19.0"
  }
}
